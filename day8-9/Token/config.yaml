# Can set testMode to property or assertion mode depending on how you want to test the system's properties
# testMode: assertion

testLimit: 500000

sender: ['0x20000', '0x30000']

psender: '0x30000'

corpusDir: echidna-coverage
